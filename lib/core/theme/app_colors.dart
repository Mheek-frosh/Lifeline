import 'package:flutter/material.dart';

/// LifeLine app color palette - White primary, Navy accents
class AppColors {
  AppColors._();

  /// Primary background - Clean white
  static const Color primary = Colors.white;

  /// Deep Professional Navy - Primary accent
  static const Color deepNavy = Color(0xFF004A7C);

  /// Dark Slate / Navy Blue - Text & headings
  static const Color lifelineText = Color(0xFF1A3B5D);

  /// Vibrant "Safety" Green - Health accent, CTAs
  static const Color safetyGreen = Color(0xFF76D300);

  /// Soft navy tint for backgrounds
  static const Color navySoft = Color(0xFFF0F4F8);

  /// Light navy for cards
  static const Color navyLight = Color(0xFFE8EEF4);

  /// Muted text
  static const Color textMuted = Color(0xFF6B7C8E);

  /// Border
  static const Color border = Color(0xFFE2E8F0);

  /// Dark section background (navy dark)
  static const Color darkSection = Color(0xFF1A2D3D);
}
